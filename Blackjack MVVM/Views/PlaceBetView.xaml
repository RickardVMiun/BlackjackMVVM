<UserControl x:Class="Blackjack_MVVM.Views.PlaceBetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Blackjack_MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border BorderBrush="Gold" BorderThickness="5" CornerRadius="5" Visibility="{Binding RulesVisible, ElementName=playrules, FallbackValue=Visible}">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black"/>
                    <GradientStop Color="#FF6B6C6B" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel>
                <local:BettingButtons HorizontalAlignment="Center" VerticalAlignment="Center"  ButtonDisabling="{Binding buttonDisabling}" />
                <local:PlaceBetButton  HorizontalAlignment="Center" VerticalAlignment="Bottom" ButtonDisabling2="{Binding buttonDisabling2}"/>
                
            </StackPanel>
            
        </Border>

    </Grid>
       
      
    
</UserControl>
